This image created on <%=@image.created_at.strftime("%B %d, %Y") %><br />
<%= image_tag(@image.image)%><br/>
COMMENTS :
<div id="comments">
  <%= render  @comments %>
</div>

<%= form_for :comment,:url => create_image_comment_user_gallery_images_path, :id=>"form_id", :remote => true do |f| %>
  <%= f.label :comment %>
  <%= f.text_field :comment %>
  <input type="hidden" name="image_id" value="<%=params[:id]%>">
  <input type="hidden" name="gallery_id" value="<%=params[:gallery_id]%>">
<%end%>


